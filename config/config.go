// Package config provides types for handling configuration parameters.
package config

// ServerConfig defines server-related constants and parameters handling.
type ServerConfig struct {
	Host string
	Port string
}
